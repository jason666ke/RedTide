# 赤潮预测系统使用说明文档

## 目录
1. [系统概述](#系统概述)
2. [环境准备](#环境准备)
3. [目录结构](#目录结构)
4. [数据要求](#数据要求)
5. [使用步骤](#使用步骤)
6. [常见问题](#常见问题)

## 系统概述
本系统是一个用于赤潮预测的完整解决方案，包含三个主要任务：
- 数据预处理：清洗和标准化原始数据
- 任务一：赤潮相关特征预测
- 任务二：赤潮发生预警
- 任务三：生态稳定性预测

## 环境准备

### 1. 基础环境要求
- Python 3.8 或更高版本
- CUDA 11.0 或更高版本（如果使用GPU）
- Git（用于版本控制）

### 2. 安装依赖包
```bash
# 创建虚拟环境
conda create -n redtide python=3.8
conda activate redtide

# 安装必要的包
pip install -r requirements.txt
```

## 目录结构
```
redtide_detect_whole_process/
├── data_process/          # 数据预处理模块
├── data_raw/             # 原始数据存放目录
├── task_1/               # 赤潮相关特征预测模块
├── task_2/               # 赤潮预警检测模块
├── task_3/               # 生态稳定性计算模块
└── run_pipeline.sh       # 主运行脚本
```

## 数据要求

### 输入数据格式
1. 原始数据应为Excel(.xlsx)格式
2. 必需的数据列：
   - 监测时间（datetime格式）
   - 叶绿素浓度
   - 水温
   - 盐度
   - 溶解氧
   - pH值
   - 浊度
   ... (其他环境参数)

### 数据存放
1. 将原始数据文件放入 `data_raw/` 目录
2. 文件命名格式：`站点名称.xlsx`
   例如：`大鹏湾下沙.xlsx`

## 使用步骤

### 1. 准备数据
1. 确保数据格式符合要求
2. 将数据文件放入正确目录

### 2. 配置参数
1. 打开 `config/station_mapping.sh`
2. 确认站点映射关系正确

### 3. 运行系统
```bash
# 添加执行权限
chmod +x run_pipeline.sh

# 运行主脚本
./run_pipeline.sh
```

### 4. 查看结果
处理结果将保存在以下位置：
- 数据预处理结果：`task_1/data_9features/`
- 预测结果：`task_2/data_final/`
- 赤潮预警结果：`task_2/results/`

## 输出说明

### 1. 预测结果格式
预测结果包含以下信息：
- 时间序列
- 预警标记（0表示正常，1表示异常）

### 2. 日志信息
- 运行日志保存在 `logs/` 目录
- 日志命名格式：`pipeline_YYYYMMDD_HHMMSS.log`

## 常见问题

### 1. 数据格式错误
**问题**：运行时提示数据格式错误
**解决**：
- 检查Excel文件格式
- 确保日期时间格式正确
- 检查是否有缺失值

### 2. GPU相关错误
**问题**：CUDA相关错误
**解决**：
- 检查CUDA版本是否兼容
- 确认GPU驱动已正确安装

### 3. 路径错误
**问题**：找不到文件或目录
**解决**：
- 确保目录结构完整
- 检查文件命名是否正确
- 确认文件权限设置

## 注意事项
1. 运行前请备份重要数据
2. 确保磁盘空间充足
3. 不要中断正在运行的程序
4. 定期检查日志文件